import Typist from 'react-text-typist';
import MainLayout from "layouts/main.js"
import PostsListings from "../components/posts-listings";
import { XlContainer } from "./_container.mdx"
import Cookies from "components/cookies"

export const meta = {
    title: "Writing | Blog | Matthew Spence",
}

<div className="container max-w-2xl mx-auto py-12">
    <XlContainer>

    <h1 className="lg:!text-8xl text-center !mb-0">Writing</h1>

    <div class="text-sm text-center text-heading">
    There are <strong>{ require.context("./../pages/writing", true, /\.\/.*\.mdx$/).keys().length + props.mediumPosts.length }</strong> written articles,
    and <a target="_blank" href="https://github.com/mrspence/mrspence/blob/main/pages/drafts">{ require.context("./../pages/drafts", true, /\.\/.*\.mdx$/).keys().length } drafts</a>. <i>Astonishing...</i>
    </div>

    <div>
        <div className="inline-block max-w-3xl">
            <Cookies/>
        </div>
    </div>

    <PostsListings mediumPosts={props.mediumPosts}/>

    </XlContainer>
</div>

export default ({ children }) => <MainLayout meta={meta}>{children}</MainLayout>

export const getStaticProps = async function() {
    return {
        props: {
            ...await require("./../static-props/medium-posts").default(),
        },
    };
}