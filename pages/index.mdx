import Link from "next/link"
import Typist from "react-text-typist"
import MainLayout from "layouts/main.js"
import PostsCards from "components/posts-cards"
import Cookies from "components/cookies"

export const meta = {
    title: "Matthew Spence",
}

<div className="container mx-auto max-w-5xl flex items-center min-h-[22rem] md:py-16">
    <div>
        <div className="pb-4">
            <small className="text-secondary uppercase text-sm font-bold">Good {(new Date).getHours() >= 12 ?  ( (new Date).getHours() >= 17 ? 'evening' : 'afternoon' ) : 'morning'}, This is Spence</small>
        </div>
        <h1 className="text-3xl md:text-6xl lg:!text-8xl lg:pb-8">
            I <Typist sentences={["create", "code",]} pauseTime={"10000"} showCursor={false} startDelay={"200"} /><span className="text-secondary"><Typist sentences={["websites. ", "apps. ", "dreams. ",]} pauseTime={"5000"} startDelay={"800"} showCursor={false} /></span>
        </h1>
        <p className="py-8 md:text-xl text-heading">
            Creating business value, through <i>strategic</i> use of digital technology.
            <br/>
            I also <Link href="/blog"><a>write about the digital world</a></Link> and post personal stories on <a target="_blank" href="https://instagram.com/watchspence">Instagram</a>.
        </p>
        <div>
            <div className="inline-block max-w-3xl">
                <Cookies/>
            </div>
        </div>
    </div>
</div>

<div className="container mx-auto pb-12">
    <div className="prose prose-xl max-w-5xl mx-auto">
        <PostsCards limit={6} mediumPosts={props.mediumPosts} />
    </div>
</div>

export default ({ children }) => <MainLayout meta={meta}>{children}</MainLayout>

export const getStaticProps = async function() {
    return {
        props: {
            ...await require("./../static-props/medium-posts").default(),
        },
    };
}
