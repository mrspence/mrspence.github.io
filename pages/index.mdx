import Link from "next/link"
import Typist from "react-text-typist"
import MainLayout from "layouts/main.js"
import PostsCards from "components/posts-cards"
import Cookies from "components/cookies"

export const meta = {
    title: "Matthew Spence",
}

<div className="container mx-auto max-w-5xl flex items-center min-h-[8rem] lg:min-h-[22rem] md:py-16">
    <div>
        <div className="pb-4">
            <small className="text-secondary uppercase text-sm font-bold">Matthew Spence</small>
        </div>
        <h1 className="text-4xl md:text-6xl lg:!text-8xl">
            <Typist sentences={["Creating", "Coding",]} pauseTime={"10000"} showCursor={false} startDelay={"200"} /><span className="text-secondary"><Typist sentences={["tech. ", "apps. ", "dreams. ",]} pauseTime={"5000"} startDelay={"800"} showCursor={false} /></span>
        </h1>
        <p className="py-8 md:text-xl text-heading !leading-tight">
            <Link href="/blog"><a>Writes</a></Link> and <Link href="/contact"><a>consults</a></Link> about <i>strategic</i> leveraging of digital tech.
        </p>
        <div>
            <div className="inline-block max-w-3xl">
                <Cookies/>
            </div>
        </div>
    </div>
</div>

<div className="container mx-auto pb-12">
    <div className="prose prose-xl max-w-5xl mx-auto">
        <PostsCards limit={6} mediumPosts={props.mediumPosts} />
    </div>
</div>

export default ({ children }) => <MainLayout meta={meta}>{children}</MainLayout>

export const getStaticProps = async function() {
    return {
        props: {
            ...await require("./../static-props/medium-posts").default(),
        },
    };
}
